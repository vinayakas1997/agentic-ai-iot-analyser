# Storage

Filesystem helper to save data in a shared storage location for the monorepo.

- Default storage root: `data` directory at repository root
- Override with environment variable: `STORAGE_ROOT`

This package exposes simple helpers for saving text and JSON files under the storage root.