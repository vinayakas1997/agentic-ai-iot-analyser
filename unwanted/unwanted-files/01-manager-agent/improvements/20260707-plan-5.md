# Plan 5 — Enhancement of the Context Side (2026-07-07)

## Goals
- Give inactive Stepper steps visible stage-colored soft pills (Planner→yellow, Execution→green)
- Apply blue glass morphism to ManagerDecisionCard dataset chips for visual distinction
- Add dataset selection, filtered columns, eye-icon expand, and horizontal scroll in ContextSection

## Phase 1 — Icon

**Edit:**
| File | Change |
|------|--------|
| `lib/icons.tsx` | Add `IconEye` component (eye SVG path + circle) for the expand/collapse toggle |

## Phase 2 — Stepper Inactive Styles

**Edit:**
| File | Change |
|------|--------|
| `ContextSection.tsx` `Stepper` | Replace dynamic `text-${color}/50` class construction with static record maps (`stepActiveClass`, `stepInactiveClass`). Inactive steps get `bg-stage-planner-soft text-stage-planner` (soft background, full-color text) instead of invisible 50% text. Active steps unchanged. |

## Phase 3 — ManagerDecisionCard Glass Chips

**Edit:**
| File | Change |
|------|--------|
| `ManagerDecisionCard.tsx` line 258 | Dataset `<span>` class from `bg-white/[0.05] border-border-2` to `bg-ic-blue-soft/60 backdrop-blur-md border-ic-blue/20` |

## Phase 4 — ContextSection Dataset Selection & Columns

**Edit:**
| File | Change |
|------|--------|
| `ContextSection.tsx` | Add `useState` for `selectedDataset` (string\|null) and `columnsExpanded` (boolean). Compute `filteredColumns` and `displayColumns`. Make dataset names clickable with amber highlight when selected. Columns header shows dataset name when filtered. Table wrapper → `overflow-x-auto`, table → `min-w-full`. Static "X more columns" → `IconEye` toggle. |
