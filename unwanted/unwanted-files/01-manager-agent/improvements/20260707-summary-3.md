# Summary 3 — UI Fix & Enhancement: Manager Agent Surface (2026-07-07)

## What Changed

### Created (1 file)
| File | Lines | Description |
|---|---|---|
| `frontend/src/components/ManagerDecisionCard.tsx` | ~390 | Consolidated decision card: resolved line, datasets, time, aims with provenance, handoff payload. Includes `InlineEdit` component for corrections and `buildProvenance` for suggested-aim→proposal mapping. |

### Edited (4 files)
| File | Change |
|---|---|
| `frontend/src/types/manager.ts` | Expanded `planner_payload` type with full handoff fields. Added `selectedProposalIndex` and `proposalProvenance` to `TurnUi`. |
| `frontend/src/sections/ContextSection.tsx` | Removed 7 redundant cards (Line match, Line, Suggested aims, In scope, Excluded, Time, No time filter, Joins). Now uses `ManagerDecisionCard` + Session meta + Datasets list + Columns table. |
| `frontend/src/sections/OutputSection.tsx` | Replaced "Active plan"/"Ready for planner" cards with `ManagerDecisionCard`. Added sticky `PipelineProgress` with 3 glass cards (Manager→Planner→Execution) with auto-status detection. Added pipelinePanelClass for sticky layout. |
| `frontend/src/sections/ChatSection.tsx` | Removed global context bar. Added per-turn context chips inside each Q&A pair showing Line, Datasets, Time, Aims, Missing fields from that turn's snapshot. |

### Deleted (0 files)
—

## Verification

- **TypeScript**: `tsc --noEmit` — clean (0 errors)
- **Vite build**: 345 modules → 378 KB JS + 25 KB CSS (gzipped: 120 KB + 5.4 KB)

## Result

| # | Issue | Resolution |
|---|-------|------------|
| 1 | Same decision in 3 places | Single `ManagerDecisionCard` in both ContextSection and OutputSection — one schema, one rendering |
| 2 | Line match + Line redundant | Line card removed; `ManagerDecisionCard` shows resolved line with reasoning |
| 3 | Suggested aims disconnected | `buildProvenance()` matches aims across suggested→proposal; badges show "→ P1,P2" links |
| 4 | No correction affordance | ✎ edit buttons on line, datasets, time fields send correction messages via existing chat mechanism |
| 5 | Handoff payload too narrow | `showHandoff={true}` renders full `planner_payload` — line, aims, datasets, joins, scope, time, alias |
| 6 | No per-turn context | Each chat bubble now shows inline context chips from its own schema/ui snapshot |
| 7 | No pipeline visibility | Sticky PipelineProgress shows Manager (orange), Planner (yellow), Execution (green) with pending/active/done states |
