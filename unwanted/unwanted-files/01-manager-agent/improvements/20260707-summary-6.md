# Summary 6 — Chat Panel Redesign (2026-07-07)

## What Changed

### Edited (4 files)
| File | Change |
|------|--------|
| `index.css` | Added `--color-user-blue`, `--color-user-blue-soft`, `--color-user-blue-line` for the user bubble accent; `--color-stage-planner-line`, `--color-stage-execution-line` for stage consistency |
| `lib/icons.tsx` | Added `IconUser` SVG component (person icon) for the user sender row |
| `lib/styles.ts` | Added 5 shared classes: `userBubbleClass`, `managerCardClass`, `qrPrimaryClass`, `qrSecondaryClass`, `composerBannerClass` |
| `sections/ChatSection.tsx` | Full rewrite (~220 lines). See breakdown below. |

### ChatSection.tsx Breakdown

**Architecture:**
- Added `OptionCard` sub-component for selectable proposal cards
- Added `chipClass()` helper for colored resolved chips (blue/amber/coral/default)
- Added `buildQuickReplies()` for context-aware action buttons

**User bubble:**
- Blue left accent border (`border-l-[3px] border-l-user-blue`), surface-1 background
- Sender row with `IconUser` + "You" in `text-user-blue`
- Resolved chips with icons (map-pin, database, clock, target) per accent:
  - Line → ic-blue, Dataset → ic-amber, Time → default, Aims → ic-coral
- Delta detection against previous turn: unchanged chips get `opacity-45`
- Diff note: "Line, dataset & time unchanged from previous turn"

**Manager card:**
- Orange left accent border (`border-l-[3px] border-l-stage-manager`), glow shadow
- Sender row with `IconCheckCircle` badge + "Manager" in `text-stage-manager`

**Plan mode** (`ui.plan`):
- Line/Time/Aims as labeled rows
- Benefits as bullet list with orange dots

**Proposals mode** (`ui.proposals`):
- Intro text with line name
- Numbered option cards: hover highlight, "select →" hint, click sends `confirm {n}`
- Per-proposal metadata: Datasets, Join, You-might-see

**Quick-reply action chips:**
- Plan: "Go — proceed" (orange primary) + "More options" + "Change something…"
- Proposals: "See more options" + "Change something…"
- Fallback: `next_step` rendered as italic text

**Composer:**
- Done state: green banner with checkmark + message, disabled input + send button
- Active state: unchanged form with `btnPrimary` send button

## Verification

- **Vite build**: 346 modules → clean (0 errors)

## Result

| # | Issue | Resolution |
|---|-------|------------|
| 1 | Manager's bubble has no stage identity | Orange left accent bar, `IconCheckCircle` badge, glow shadow — consistent with Context panel |
| 2 | Plan and proposals rendered as raw markdown | Structured Plan view (Line/Time/Aims + Benefits) and Proposals view (numbered option cards) from `turn.ui` |
| 3 | Proposals are plain text, not selectable | `OptionCard` component with hover effect, "select →" hint, numbered chip, click sends `confirm {n}` |
| 4 | Quick actions buried in prose | `buildQuickReplies()` generates actual action chips (Go, More options, Change something) |
| 5 | Resolved chips repeat at full opacity | Delta detection dims unchanged chips (`opacity-45`) + italic diff note |
| 6 | Composer end-state is italic placeholder | Green banner with checkmark + disabled input/send |
